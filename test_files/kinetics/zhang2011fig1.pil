# cooperative hybridization

length 1 = 8
length 2 = 18
length 3 = 18
length 4 = 8

target_T1 = 1 2
target_T2 = 3 4
complex_D1 = 2( 3( + 4* ) ) 1*

# this does fine with and without -c, but no net reaction occurs, but that's correct for the low-concentration limit
# ../../enumerator.py --release-cutoff 8 zhang2011fig1.pil -r 

# this should create some additional resting state complexes, but it doesn't.  even increasing k-fast doesn't have an effect.
# ../../enumerator.py --release-cutoff 8 zhang2011fig1.pil -r --k-fast 0.01
